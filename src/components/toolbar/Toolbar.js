import React from 'react'

/**
 * Toolbar
 *  Tools to filter Jalons, statistize views, ...
 * @version 1.0.0
 */

const Toolbar = () => (
    <div>
        Toolbar works
    </div>
)

export default Toolbar